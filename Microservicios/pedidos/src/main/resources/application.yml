server:
  port: 8086
spring:
  application:
    name: PEDIDOS
  datasource:
    url: jdbc:mysql://localhost:3306/tienda_pedidos?createDatabaseIfNotExist=true
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
