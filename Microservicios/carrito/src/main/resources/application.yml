server:
  port: 8084
spring:
  application:
    name: CARRITO
  datasource:
    url: jdbc:mysql://localhost:3306/tienda_carrito?createDatabaseIfNotExist=true
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
