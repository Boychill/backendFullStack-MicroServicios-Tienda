meta {
  name: AgregarItem
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/carrito/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "productoId": 1,
    "cantidad": 2
  }
}
